## **Часть 1: Базовый шаблон Worker Pool**

```go
package main

import (
    "fmt"
    "time"
)

// Функция worker представляет одного работника
func worker(id int, tasks <-chan int, results chan<- int) {
    for task := range tasks {
        fmt.Printf("Worker %d processing task %d\n", id, task)

        // Симулируем работу (1 секунда)
        time.Sleep(1 * time.Second)

        // Отправляем результат (задача * 2)
        results <- task * 2
    }
}

func main() {
    // Фиксируем количество работников и задач
    numWorkers := 3
    numJobs := 10

    // Создаем каналы с буфером по количеству задач
    tasks := make(chan int, numJobs)
    results := make(chan int, numJobs)

    // Создаем работников (горутины)
    for i := 0; i < numWorkers; i++ {
        go worker(i, tasks, results)
    }

    // Отправляем задачи в канал tasks
    for i := 0; i < numJobs; i++ {
        tasks <- i
    }

    // Закрываем канал tasks (больше задач не будет)
    close(tasks)

    // Собираем результаты
    for i := 0; i < numJobs; i++ {
        result := <-results
        fmt.Println("Result:", result)
    }
}
```

**Вывод в консоли (пример):**

```
Worker 0 processing task 0
Worker 1 processing task 1
Worker 2 processing task 2
Worker 0 processing task 3
Worker 1 processing task 4
Result: 0
Result: 2
Result: 4
Result: 6
Worker 2 processing task 5
Worker 0 processing task 6
Result: 8
Result: 10
Worker 1 processing task 7
Result: 12
Worker 2 processing task 8
Result: 14
Worker 0 processing task 9
Result: 16
Result: 18
```

**Объяснение:**

1. Создаем 3 работника (горутины) и 10 задач
2. Каждый работник ждет задачи из канала `tasks`
3. Когда задача поступает, работник обрабатывает ее (1 секунда) и отправляет результат в канал `results`
4. Главная программа собирает результаты из канала `results`

# ВАЖНО

Отличный вопрос. Ты сейчас упёрся **в самую важную и неочевидную особенность Go-scheduler’а и каналов.**
И это ключ к пониманию почему Go иногда “ведёт себя странно”.

Разберём это медленно и по шагам.

---

# 🔴 Главный парадокс

Ты ожидаешь:

> «Горутины спят 1 сек → отправляют в results → main уже ждёт `<-results>` → значит результат должен печататься сразу»

**Логически — да.
Физически (на уровне scheduler'а Go) — НЕТ.**

И вот почему.

---

# 🧠 Что на самом деле происходит

У тебя:

• 3 worker-горутины
• 1 main-горутина
• 1 OS-поток (скорее всего, GOMAXPROCS = 1)

👉 Все 4 горутины дерутся за **один поток**.

---

## Таймлайн реального исполнения

### Шаг 1

Workers стартуют и печатают:

```
Worker 0 processing task 1
Worker 2 processing task 0
Worker 1 processing task 2
```

Все 3 сделали `Sleep(1s)` →
**Go runtime помечает их как “спящие”** и возвращает поток.

---

### Шаг 2

main тоже начинает читать:

```go
result := <-results
```

❗ И БЛОКИРУЕТСЯ (нет результатов ещё).

➡ Поток полностью свободен.
➡ Все горутины сейчас либо sleeping, либо blocked.

---

### Шаг 3 — через 1 секунду

Три worker’а **одновременно просыпаются**.

Но поток ОДИН.

Scheduler говорит:

> «Окей, кто первый?»

И начинает выполнять ИХ, **а не main**.

---

### Шаг 4 — critical момент

Worker делает:

```go
results <- task*2
```

Но **канал results БУФЕРИЗОВАННЫЙ** (`make(chan int, numJobs)`).

👉 Это значит:

> Отправка **НЕ блокируется**,
> она просто кладёт значение в буфер и worker продолжает работу.

❗❗❗
То есть main даже НЕ ПРОСЫПАЕТСЯ.

Worker мгновенно берёт **следующую задачу** и печатает:

```
Worker 1 processing task 3
Worker 0 processing task 4
Worker 2 processing task 5
```

---

### Шаг 5 — только потом

Когда workers снова ушли в `Sleep`, поток наконец достаётся main.

И ТОЛЬКО ТЕПЕРЬ:

```
Result: 4
Result: 2
Result: 0
```

---

# 💥 Вот ключевая причина

| Фактор                      | Что он делает                              |
| --------------------------- | ------------------------------------------ |
| results **buffered**        | main НЕ разблокируется при send            |
| GOMAXPROCS=1                | только 1 горутина исполняется одновременно |
| Worker быстрее main         | workers полностью “забивают” поток         |
| Scheduler НЕ fairness-based | он не обязан “пускать main”                |

---

# ⚡ Эксперимент (сломай иллюзию)

Поменяй:

```go
results := make(chan int) // UNBUFFERED
```

И ты УВИДИШЬ:

```
Worker 0 processing task 1
Worker 2 processing task 0
Worker 1 processing task 2
Result: 2
Result: 0
Result: 4
Worker 1 processing task 3
...
```

🔥 Потому что теперь:

> `results <- x` **БЛОКИРУЕТ worker**,
> пока main не прочитает → происходит честная передача.

---

# 🎯 Итог (важнейшее правило Go)

> **Buffered channel = очередь** > **Unbuffered channel = синхронизация**

Ты сейчас впервые столкнулся с этим различием на уровне scheduler’а.

---

Отлично, объясню этот код максимально подробно, шаг за шагом. Это **Worker Pool** (пул работников) - одна из самых важных концепций в Go для параллельной обработки задач.

## **Шаг 1: Объясняю, что такое Worker Pool (на пальцах)**

Представьте фабрику:

- **3 работника** (горутины) на конвейере
- **10 деталей** (задач), которые нужно обработать
- **Конвейер** (канал `tasks`) подает детали работникам
- **Готовые детали** (результаты) складываются на другой конвейер (`results`)

## **Шаг 2: Детальный разбор функции `worker`**

```go
// worker - это один работник на фабрике
// id - номер работника (0, 1, 2)
// tasks <-chan int - конвейер, ПО КОТОРОМУ ПРИХОДЯТ детали (только читать!)
// results chan<- int - конвейер, В КОТОРЫЙ СКЛАДЫВАЮТ готовые детали (только писать!)
func worker(id int, tasks <-chan int, results chan<- int) {
    // Работник стоит у конвейера и ждет детали
    // range по каналу БЛОКИРУЕТСЯ, пока есть детали или пока конвейер не остановят
    for task := range tasks {
        // Получил деталь! task - это номер детали (0, 1, 2, ... 9)
        fmt.Printf("Worker %d processing task %d\n", id, task)

        // Обрабатывает деталь 1 секунду (имитация работы)
        time.Sleep(1 * time.Second)

        // Готово! Кладем результат на другой конвейер
        // Результат = деталь * 2 (например, деталь №3 → результат 6)
        results <- task * 2
    }
    // Когда конвейер tasks закрывают, цикл for range завершается
    // Работник уходит домой (горутина завершается)
}
```

**Ключевые моменты:**

- `<-chan int` - можно только ЧИТАТЬ из этого канала
- `chan<- int` - можно только ПИСАТЬ в этот канал
- `for task := range tasks` - магия Go! Блокируется, ждет новую задачу

## **Шаг 3: Детальный разбор функции `main`**

### **Часть 3.1: Настройка фабрики**

```go
func main() {
    // Сколько работников на фабрике?
    numWorkers := 3

    // Сколько деталей нужно обработать?
    numJobs := 10

    // Создаем КОНВЕЙЕР ДЛЯ ДЕТАЛЕЙ (буферизованный канал)
    // Буфер на 10 деталей - можно положить 10 деталей без ожидания
    tasks := make(chan int, numJobs)

    // Создаем КОНВЕЙЕР ДЛЯ ГОТОВЫХ ИЗДЕЛИЙ (тоже буферизованный)
    results := make(chan int, numJobs)
```

**Что такое буферизованный канал?**

```go
// БЕЗ буфера (unbuffered):
// tasks <- 1 // БЛОКИРУЕТСЯ, пока кто-то не прочитает!
//
// С буфером 10 (buffered):
// tasks <- 1 // Не блокируется! Просто кладет в буфер
// tasks <- 2 // И так до 10 деталей
// tasks <- 11 // А вот это заблокируется, если никто не забирает
```

### **Часть 3.2: Наняли работников**

```go
    // Наняли 3 работников (запустили 3 горутины)
    for i := 0; i < numWorkers; i++ {
        // Каждый работник:
        // - имеет номер i (0, 1, 2)
        // - стоит у конвейера tasks (ждет детали)
        // - складывает результат в конвейер results
        go worker(i, tasks, results)
    }

    // Что происходит СЕЙЧАС:
    // 3 горутины ЗАПУЩЕНЫ и ЖДУТ у пустого конвейера tasks
    // Они ВИСЯТ на строке: for task := range tasks
    // Потому что range по пустому каналу БЛОКИРУЕТСЯ
```

### **Часть 3.3: Загружаем детали на конвейер**

```go
    // Начальник (main горутина) начинает класть детали на конвейер
    for i := 0; i < numJobs; i++ {
        // Кладем деталь №0 на конвейер tasks
        // tasks <- 0 // Деталь №0
        // tasks <- 1 // Деталь №1
        // ...
        // tasks <- 9 // Деталь №9
        tasks <- i
    }

    // Что происходит:
    // 1. Кладем деталь №0 → работник №0 забирает её сразу!
    // 2. Кладем деталь №1 → работник №1 забирает её сразу!
    // 3. Кладем деталь №2 → работник №2 забирает её сразу!
    // 4. Кладем деталь №3 → ВСЕ работники ЗАНЯТЫ! Деталь ждет в буфере
    // 5. И так все 10 деталей...
```

### **Часть 3.4: Останавливаем конвейер**

```go
    // Все детали загружены! Останавливаем конвейер
    close(tasks)

    // Важно: close() НЕ означает "уничтожить канал"
    // Это значит: "больше деталей не будет, можно уходить"
    // Работники закончат текущую деталь и завершат цикл for range
```

### **Часть 3.5: Собираем готовые изделия**

```go
    // Теперь собираем готовые изделия с конвейера results
    for i := 0; i < numJobs; i++ {
        // Ждем готовое изделие с конвейера results
        // <-results БЛОКИРУЕТСЯ, пока работник не положит результат
        result := <-results

        // Печатаем результат
        fmt.Println("Result:", result)
    }

    // Всё! Программа завершается
}
```

## **Шаг 4: Визуализируем ВСЁ, что происходит**

Давайте представим это в виде таблицы времени:

```
Время    | Событие
---------|------------------------------------------------
T = 0.0s | Запустили 3 работников (горутины 0, 1, 2)
         | Все они ЖДУТ у пустого конвейера tasks

T = 0.0s | Кладем деталь №0 → работник 0 забирает
         | Кладем деталь №1 → работник 1 забирает
         | Кладем деталь №2 → работник 2 забирает

T = 0.0s | Кладем деталь №3 → ЖДЕТ в буфере (все заняты)
         | Кладем деталь №4 → ЖДЕТ в буфере
         | ... так до детали №9

T = 1.0s | Работник 0 закончил деталь №0!
         | Результат 0*2=0 кладет на results
         | Работник 0 берет деталь №3 из буфера!

T = 1.0s | Работник 1 закончил деталь №1!
         | Результат 1*2=2 кладет на results
         | Работник 1 берет деталь №4 из буфера!

T = 1.0s | Работник 2 закончил деталь №2!
         | Результат 2*2=4 кладет на results3
         | Работник 2 берет деталь №5 из буфера!

T = 1.0s | Главная программа: <-results получает 0
         | Печатает: Result: 0

T = 1.0s | Главная программа: <-results получает 2
         | Печатает: Result: 2

... и так далее ...
```

## **Шаг 5: Показываю код с отладочной печатью**

```go
package main

import (
    "fmt"
    "time"
)

func worker(id int, tasks <-chan int, results chan<- int) {
    fmt.Printf("[Worker %d] Пришел на работу, жду у конвейера...\n", id)

    for task := range tasks {
        fmt.Printf("[Worker %d] Получил деталь №%d с конвейера\n", id, task)

        fmt.Printf("[Worker %d] Обрабатываю деталь №%d (1 секунда)...\n", id, task)
        time.Sleep(1 * time.Second)

        result := task * 2
        fmt.Printf("[Worker %d] Закончил! Кладу результат %d на results конвейер\n", id, result)
        results <- result
    }

    fmt.Printf("[Worker %d] Конвейер остановили, ухожу домой\n", id)
}

func main() {
    fmt.Println("=== НАЧАЛО РАБОЧЕГО ДНЯ ===")

    numWorkers := 3
    numJobs := 10

    tasks := make(chan int, numJobs)
    results := make(chan int, numJobs)

    fmt.Printf("\nНаняли %d работников:\n", numWorkers)
    for i := 0; i < numWorkers; i++ {
        go worker(i, tasks, results)
    }

    // Даем время работникам "дойти до конвейера"
    time.Sleep(100 * time.Millisecond)

    fmt.Printf("\nЗагружаем %d деталей на конвейер:\n", numJobs)
    for i := 0; i < numJobs; i++ {
        fmt.Printf("Кладу деталь №%d на конвейер\n", i)
        tasks <- i
    }

    fmt.Println("\nВсе детали загружены! Закрываем конвейер...")
    close(tasks)

    fmt.Println("\nСобираем готовые изделия:")
    for i := 0; i < numJobs; i++ {
        result := <-results
        fmt.Printf("Получил готовое изделие: %d\n", result)
    }

    fmt.Println("\n=== КОНЕЦ РАБОЧЕГО ДНЯ ===")
    // Даем время горутинам написать последние сообщения
    time.Sleep(100 * time.Millisecond)
}
```

## **Шаг 6: Что будет на выходе (пример)**

```
=== НАЧАЛО РАБОЧЕГО ДНЯ ===

Наняли 3 работников:
[Worker 0] Пришел на работу, жду у конвейера...
[Worker 1] Пришел на работу, жду у конвейера...
[Worker 2] Пришел на работу, жду у конвейера...

Загружаем 10 деталей на конвейер:
Кладу деталь №0 на конвейер
[Worker 0] Получил деталь №0 с конвейера
[Worker 0] Обрабатываю деталь №0 (1 секунда)...
Кладу деталь №1 на конвейер
[Worker 1] Получил деталь №1 с конвейера
[Worker 1] Обрабатываю деталь №1 (1 секунда)...
Кладу деталь №2 на конвейер
[Worker 2] Получил деталь №2 с конвейера
[Worker 2] Обрабатываю деталь №2 (1 секунда)...
Кладу деталь №3 на конвейер
Кладу деталь №4 на конвейер
Кладу деталь №5 на конвейер
Кладу деталь №6 на конвейер
Кладу деталь №7 на конвейер
Кладу деталь №8 на конвейер
Кладу деталь №9 на конвейер

Все детали загружены! Закрываем конвейер...

Собираем готовые изделия:
[Worker 0] Закончил! Кладу результат 0 на results конвейер
[Worker 0] Получил деталь №3 с конвейера
[Worker 0] Обрабатываю деталь №3 (1 секунда)...
Получил готовое изделие: 0
[Worker 1] Закончил! Кладу результат 2 на results конвейер
[Worker 1] Получил деталь №4 с конвейера
[Worker 1] Обрабатываю деталь №4 (1 секунда)...
Получил готовое изделие: 2
[Worker 2] Закончил! Кладу результат 4 на results конвейер
[Worker 2] Получил деталь №5 с конвейера
[Worker 2] Обрабатываю деталь №5 (1 секунда)...
Получил готовое изделие: 4
... и так далее ...

=== КОНЕЦ РАБОЧЕГО ДНЯ ===
```

## **Шаг 7: Ключевые моменты для понимания**

1. **`<-chan int` и `chan<- int`** - это не просто синтаксис, а **гарантии безопасности**:

   - Работник НЕ МОЖЕТ положить задачу в `tasks` (только читать)
   - Работник НЕ МОЖЕТ забрать результат из `results` (только писать)

2. **`for task := range tasks`** - это магия Go:

   - Автоматически ждет новые задачи
   - Автоматически завершается при `close(tasks)`

3. **Буферизованные каналы** предотвращают блокировки:

   - Можно положить 10 задач БЕЗ ожидания
   - Работники успеют забрать их потом

4. **`close(tasks)`** - это сигнал "работа закончена":

   - Без этого работники будут ждать ВЕЧНО
   - Важно закрывать канал ПОСЛЕ загрузки всех задач

5. **Порядок вывода НЕ гарантирован**:
   - Работник 0 может закончить позже работника 1
   - Результаты могут приходить в разном порядке

## **Шаг 8: Почему это полезно?**

Без Worker Pool:

```go
// ПЛОХО: 1000 горутин одновременно!
for i := 0; i < 1000; i++ {
    go processTask(i) // 1000 одновременных запросов к БД!
}
// Сервер упадет от нагрузки
```

С Worker Pool:

```go
// ХОРОШО: всего 10 горутин обрабатывают 1000 задач
for i := 0; i < 10; i++ {
    go worker(i, tasks, results)
}
// Контролируемая нагрузка, не упадет
```

## **Итог в трех предложениях:**

1. **Worker Pool** - это группа горутин, которые ждут задачи из общего канала
2. **Главная программа** кладет задачи в канал, работники забирают и обрабатывают
3. **Результаты** возвращаются в другой канал, главная программа их собирает

Это **паттерн для контроля параллелизма** - мы решаем, сколько одновременно может работать горутин, а не запускаем их все сразу.

## **Часть 2: Как работает Worker Pool (визуализация)**

```go
package main

import (
    "fmt"
    "time"
)

func worker(id int, tasks <-chan int, results chan<- int) {
    for task := range tasks {
        fmt.Printf("[Worker %d] начал задачу %d\n", id, task)
        time.Sleep(1 * time.Second)
        fmt.Printf("[Worker %d] завершил задачу %d\n", id, task)
        results <- task * 2
    }
    fmt.Printf("[Worker %d] завершил работу (канал закрыт)\n", id)
}

func main() {
    fmt.Println("=== СТАРТ ПРОГРАММЫ ===")

    numWorkers := 3
    numJobs := 5

    tasks := make(chan int, numJobs)
    results := make(chan int, numJobs)

    // Запускаем работников
    fmt.Println("Запускаю работников...")
    for i := 0; i < numWorkers; i++ {
        go worker(i, tasks, results)
    }

    // Отправляем задачи
    fmt.Println("Отправляю задачи...")
    for i := 0; i < numJobs; i++ {
        fmt.Printf("Отправляю задачу %d в канал\n", i)
        tasks <- i
        time.Sleep(200 * time.Millisecond) // Пауза между отправкой задач
    }

    // Закрываем канал
    fmt.Println("Закрываю канал tasks...")
    close(tasks)

    // Собираем результаты
    fmt.Println("Собираю результаты...")
    for i := 0; i < numJobs; i++ {
        result := <-results
        fmt.Printf("Получен результат: %d\n", result)
    }

    fmt.Println("=== ЗАВЕРШЕНИЕ ПРОГРАММЫ ===")
}
```

**Вывод в консоли:**

```
=== СТАРТ ПРОГРАММЫ ===
Запускаю работников...
Отправляю задачи...
Отправляю задачу 0 в канал
[Worker 0] начал задачу 0
Отправляю задачу 1 в канал
[Worker 1] начал задачу 1
Отправляю задачу 2 в канал
[Worker 2] начал задачу 2
Отправляю задачу 3 в канал
Отправляю задачу 4 в канал
Закрываю канал tasks...
Собираю результаты...
[Worker 0] завершил задачу 0
Получен результат: 0
[Worker 0] начал задачу 3
[Worker 1] завершил задачу 1
Получен результат: 2
[Worker 1] начал задачу 4
[Worker 2] завершил задачу 2
Получен результат: 4
[Worker 2] завершил работу (канал закрыт)
[Worker 0] завершил задачу 3
Получен результат: 6
[Worker 1] завершил задачу 4
Получен результат: 8
[Worker 0] завершил работу (канал закрыт)
[Worker 1] завершил работу (канал закрыт)
=== ЗАВЕРШЕНИЕ ПРОГРАММЫ ===
```

## **Часть 3: Реальный пример - продажа билетов**

```go
package main

import (
    "fmt"
    "time"
)

// Структура для запроса на покупку билета
type TicketRequest struct {
    PersonID    int
    NumTickets  int
    Cost        int
}

// Обработчик билетов (работник)
func ticketProcessor(id int, requests <-chan TicketRequest, results chan<- int) {
    for req := range requests {
        fmt.Printf("[Processor %d] Processing %d tickets for person %d, total cost: $%d\n",
            id, req.NumTickets, req.PersonID, req.Cost)

        // Симулируем обработку (1 секунда)
        time.Sleep(1 * time.Second)

        // Отправляем обработанный PersonID
        results <- req.PersonID
    }
}

func main() {
    fmt.Println("=== TICKET SELLING SYSTEM ===")

    numRequests := 5
    ticketPrice := 50

    // Создаем каналы
    ticketRequests := make(chan TicketRequest, numRequests)
    ticketResults := make(chan int, numRequests)

    // Запускаем 3 обработчика (работника)
    for i := 0; i < 3; i++ {
        go ticketProcessor(i, ticketRequests, ticketResults)
    }

    // Отправляем запросы на покупку билетов
    fmt.Println("\nSending ticket requests...")
    for i := 0; i < numRequests; i++ {
        req := TicketRequest{
            PersonID:   i + 100, // ID человека (100, 101, 102...)
            NumTickets: i + 1,   // Количество билетов (1, 2, 3...)
            Cost:       (i + 1) * ticketPrice, // Стоимость
        }
        fmt.Printf("Sending request: Person %d wants %d tickets ($%d)\n",
            req.PersonID, req.NumTickets, req.Cost)
        ticketRequests <- req
        time.Sleep(300 * time.Millisecond) // Пауза между запросами
    }

    // Закрываем канал запросов
    close(ticketRequests)

    // Собираем результаты
    fmt.Println("\nCollecting results...")
    for i := 0; i < numRequests; i++ {
        processedID := <-ticketResults
        fmt.Printf("Ticket for person %d processed successfully\n", processedID)
    }

    fmt.Println("\n=== ALL REQUESTS PROCESSED ===")
}
```

**Вывод в консоли:**

```
=== TICKET SELLING SYSTEM ===

Sending ticket requests...
Sending request: Person 100 wants 1 tickets ($50)
[Processor 0] Processing 1 tickets for person 100, total cost: $50
Sending request: Person 101 wants 2 tickets ($100)
[Processor 1] Processing 2 tickets for person 101, total cost: $100
Sending request: Person 102 wants 3 tickets ($150)
[Processor 2] Processing 3 tickets for person 102, total cost: $150
Sending request: Person 103 wants 4 tickets ($200)
Sending request: Person 104 wants 5 tickets ($250)

Collecting results...
[Processor 0] completed processing for person 100
Ticket for person 100 processed successfully
[Processor 0] Processing 4 tickets for person 103, total cost: $200
[Processor 1] completed processing for person 101
Ticket for person 101 processed successfully
[Processor 1] Processing 5 tickets for person 104, total cost: $250
[Processor 2] completed processing for person 102
Ticket for person 102 processed successfully
[Processor 2] finished work (no more requests)
[Processor 0] completed processing for person 103
Ticket for person 103 processed successfully
[Processor 1] completed processing for person 104
Ticket for person 104 processed successfully

=== ALL REQUESTS PROCESSED ===
```

## **Часть 4: Как выглядит обработка без Worker Pool**

```go
package main

import (
    "fmt"
    "time"
)

func processTaskWithoutPool(task int) {
    fmt.Printf("Начата обработка задачи %d\n", task)
    time.Sleep(1 * time.Second)
    fmt.Printf("Завершена обработка задачи %d\n", task)
}

func main() {
    fmt.Println("=== БЕЗ WORKER POOL ===")
    fmt.Println("(Каждая задача запускает свою горутину)")

    numTasks := 10

    for i := 0; i < numTasks; i++ {
        go processTaskWithoutPool(i)
    }

    // Ждем завершения всех горутин
    time.Sleep(2 * time.Second)
    fmt.Println("\n=== ПРОБЛЕМА БЕЗ POOL ===")
    fmt.Println("1. Создано 10 горутин одновременно")
    fmt.Println("2. Может перегрузить систему")
    fmt.Println("3. Нет контроля над количеством одновременных задач")
}
```

**Вывод в консоли:**

```
=== БЕЗ WORKER POOL ===
(Каждая задача запускает свою горутину)
Начата обработка задачи 0
Начата обработка задачи 1
Начата обработка задачи 2
Начата обработка задачи 3
Начата обработка задачи 4
Начата обработка задачи 5
Начата обработка задачи 6
Начата обработка задачи 7
Начата обработка задачи 8
Начата обработка задачи 9
Завершена обработка задачи 0
Завершена обработка задачи 1
Завершена обработка задачи 2
Завершена обработка задачи 3
Завершена обработка задачи 4
Завершена обработка задачи 5
Завершена обработка задачи 6
Завершена обработка задачи 7
Завершена обработка задачи 8
Завершена обработка задачи 9

=== ПРОБЛЕМА БЕЗ POOL ===
1. Создано 10 горутин одновременно
2. Может перегрузить систему
3. Нет контроля над количеством одновременных задач
```

## **Часть 5: Сравнение производительности**

```go
package main

import (
    "fmt"
    "time"
)

func main() {
    fmt.Println("=== СРАВНЕНИЕ ПРОИЗВОДИТЕЛЬНОСТИ ===")

    // Тест 1: Worker Pool (3 работника, 10 задач)
    fmt.Println("\n1. Worker Pool (3 работника, 10 задач):")
    start := time.Now()

    numWorkers := 3
    numJobs := 10
    tasks := make(chan int, numJobs)
    results := make(chan int, numJobs)

    // Запуск работников
    for i := 0; i < numWorkers; i++ {
        go func(id int) {
            for task := range tasks {
                time.Sleep(1 * time.Second) // Каждая задача - 1 секунда
                results <- task
            }
        }(i)
    }

    // Отправка задач
    for i := 0; i < numJobs; i++ {
        tasks <- i
    }
    close(tasks)

    // Сбор результатов
    for i := 0; i < numJobs; i++ {
        <-results
    }

    elapsedPool := time.Since(start)
    fmt.Printf("Время выполнения: %v\n", elapsedPool)
    fmt.Printf("Теоретическое время: ~%v (3 параллельных задачи)\n",
               4*time.Second) // 10 задач / 3 работника ≈ 4 секунды

    // Тест 2: Без Worker Pool (10 горутин одновременно)
    fmt.Println("\n2. Без Worker Pool (10 горутин одновременно):")
    start = time.Now()

    done := make(chan bool, 10)
    for i := 0; i < 10; i++ {
        go func(id int) {
            time.Sleep(1 * time.Second)
            done <- true
        }(i)
    }

    // Ожидание завершения всех горутин
    for i := 0; i < 10; i++ {
        <-done
    }

    elapsedNoPool := time.Since(start)
    fmt.Printf("Время выполнения: %v\n", elapsedNoPool)

    fmt.Println("\n=== ВЫВОД ===")
    fmt.Println("Worker Pool дольше (~4с), но контролирует нагрузку")
    fmt.Println("Без Pool быстрее (~1с), но может перегрузить систему")
}
```

**Вывод в консоли:**

```
=== СРАВНЕНИЕ ПРОИЗВОДИТЕЛЬНОСТИ ===

1. Worker Pool (3 работника, 10 задач):
Время выполнения: 4.002456789s
Теоретическое время: ~4s (3 параллельных задачи)

2. Без Worker Pool (10 горутин одновременно):
Время выполнения: 1.001234567s

=== ВЫВОД ===
Worker Pool дольше (~4с), но контролирует нагрузку
Без Pool быстрее (~1с), но может перегрузить систему
```

## **Часть 6: Практический пример с Graceful Shutdown**

```go
package main

import (
    "fmt"
    "time"
)

func worker(id int, tasks <-chan int, done chan<- bool, stop <-chan bool) {
    for {
        select {
        case task, ok := <-tasks:
            if !ok {
                // Канал tasks закрыт
                fmt.Printf("[Worker %d] Канал закрыт, завершаю работу\n", id)
                done <- true
                return
            }
            fmt.Printf("[Worker %d] Обрабатываю задачу %d\n", id, task)
            time.Sleep(2 * time.Second)
            fmt.Printf("[Worker %d] Завершил задачу %d\n", id, task)

        case <-stop:
            // Получен сигнал остановки
            fmt.Printf("[Worker %d] Получил сигнал остановки\n", id)
            done <- true
            return
        }
    }
}

func main() {
    fmt.Println("=== WORKER POOL С GRACEFUL SHUTDOWN ===")

    tasks := make(chan int, 10)
    done := make(chan bool, 3)     // Для сигналов завершения работников
    stop := make(chan bool)        // Для сигнала остановки

    // Запускаем 3 работника
    for i := 0; i < 3; i++ {
        go worker(i, tasks, done, stop)
    }

    // Отправляем задачи
    for i := 0; i < 5; i++ {
        tasks <- i
        time.Sleep(500 * time.Millisecond)
    }

    // Ждем 3 секунды
    time.Sleep(3 * time.Second)

    // Отправляем сигнал остановки (вместо закрытия канала)
    fmt.Println("\nОтправляю сигнал остановки работникам...")
    for i := 0; i < 3; i++ {
        stop <- true
    }

    // Ждем завершения всех работников
    for i := 0; i < 3; i++ {
        <-done
    }

    fmt.Println("Все работники завершили работу")
    fmt.Println("Программа завершена корректно")
}
```

**Вывод в консоли:**

```
=== WORKER POOL С GRACEFUL SHUTDOWN ===
[Worker 0] Обрабатываю задачу 0
[Worker 1] Обрабатываю задачу 1
[Worker 2] Обрабатываю задачу 2
[Worker 0] Завершил задачу 0
[Worker 0] Обрабатываю задачу 3
[Worker 1] Завершил задачу 1
[Worker 1] Обрабатываю задачу 4
[Worker 2] Завершил задачу 2

Отправляю сигнал остановки работникам...
[Worker 0] Получил сигнал остановки
[Worker 1] Получил сигнал остановки
[Worker 2] Получил сигнал остановки
Все работники завершили работу
Программа завершена корректно
```

## **Итог лекции:**

1. **Worker Pool** - это группа горутин (работников), которые обрабатывают очередь задач
2. **Зачем нужен Worker Pool:**
   - Контроль количества одновременных горутин
   - Эффективное распределение задач
   - Масштабируемость
3. **Основные компоненты:**
   - Задачи (tasks) - что нужно обработать
   - Работники (workers) - горутины, обрабатывающие задачи
   - Очередь задач (task queue) - канал для передачи задач
   - Результаты (results) - канал для возврата результатов
4. **Паттерн использования:**

   ```go
   // 1. Создать каналы
   tasks := make(chan Task)
   results := make(chan Result)

   // 2. Запустить работников
   for i := 0; i < numWorkers; i++ {
       go worker(i, tasks, results)
   }

   // 3. Отправить задачи
   for _, task := range taskList {
       tasks <- task
   }
   close(tasks)

   // 4. Собрать результаты
   for i := 0; i < len(taskList); i++ {
       <-results
   }
   ```

5. **Лучшие практики:**
   - Ограничивать количество работников
   - Корректно завершать работу (graceful shutdown)
   - Использовать таймауты
   - Мониторить производительность

**Ключевое преимущество:** Worker Pool позволяет обрабатывать множество задач с контролем над нагрузкой на систему.
