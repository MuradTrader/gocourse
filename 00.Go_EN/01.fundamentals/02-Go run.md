## Introduction: Why It’s Important to Understand the Difference Between `go run` and `go build`

- **What is Go?** Go (or Golang) is a programming language and a set of tools that let you write a program (code) and turn it into an executable program (a binary) that can be run on a computer.

- **What does the `go run` command do?** It compiles your code on the fly and immediately runs it, leaving no file behind that you could run again and again.

- **What does the `go build` command do?** It compiles your code and saves the result as a permanent program (an application file) that you can run as many times as you like.

If these terms (compilation, binary, HDD/SSD, RAM) don’t mean anything to you yet — don’t worry. We’ll break everything down step by step in what follows.

---

## 1. Why do people even ask: “Why use `go run` if `go build` exists?”

> **The author says:**
> “Why do we use `go run` when we could simply run `go build`? It’s simple. `go build` creates a ‘permanent’ program (a binary), whereas `go run` creates a ‘temporary’ program, immediately runs it, and doesn’t leave anything on the disk.

### 1.1. A Brief Overview of «Permanent» and «Temporary» Binaries

1. **`go build` (permanent binary)**

- When you run `go build`, the Go utility reads your files with the `.go` extension (source files), converts them into machine code (instructions for the processor), and writes the resulting file (called a «binary») next to your source files (usually in the same folder).

- This file remains on the disk until you delete it. When you run such a program again, no recompilation is needed — you simply launch the already compiled program.

---

**2. go run (temporary binary)**

- When you run `go run`, the Go toolchain also reads the source files and compiles them into machine code, but it does not place the result in the directory where your `.go` files are located. Instead, Go quickly creates a **temporary executable file** somewhere “for a short time” (for example, in the `/tmp` directory, or it may even keep it only in memory).

- As soon as the compiled program finishes its execution, Go automatically deletes this temporary file. In the directory with your `.go` source files, nothing remains except the source files themselves.

<details>
  <summary><b>📖 Подробное разборное объяснение (кликни)</b></summary>

## Заголовок

### **2. go run (temporary binary)**

- **go run** — команда Go
  - **go** — инструмент (Go tool)
  - **run** — _запускать_

- **temporary** — временный
  - от слова **temp** = временно
  - часто встречается в IT: `temp file`, `temporary directory`

- **binary** — бинарный файл (исполняемый файл)
  - программа в виде машинного кода
  - то, что реально запускается ОС

📌 **temporary binary** = _временный исполняемый файл_

---

## Предложение 1

### **When you run go run,**

- **When** — когда
  👉 вводит условие / момент времени
- **you** — ты / вы
- **run** — запускаешь
- **go run** — команда

📌 **When you run go run**
= _Когда ты запускаешь команду go run_

---

### **the Go toolchain also reads the source files**

- **the** — _определённый артикль_
  👉 речь о конкретном инструменте Go

- **Go toolchain** — инструменты Go
  - **tool** — инструмент
  - **chain** — цепочка
    👉 _набор инструментов компиляции_

- **also** — тоже, также
  👉 важное слово, показывает **дополнение**

- **reads** — читает
  - **read → reads** (потому что `toolchain` — это **it**)

- **the source files** — исходные файлы
  - **source** — исходный
  - **files** — файлы

📌
**the Go toolchain also reads the source files**
= _инструменты Go также читают исходные файлы_

---

### **and compiles them into machine code,**

- **and** — и
- **compiles** — компилирует
  - compile → compiles (3-е лицо)

- **them** — их
  👉 заменяет `source files`
- **into** — в (в результате превращения!)
  👉 очень важно:
  - **into** = превращение

- **machine code** — машинный код

📌
**compiles them into machine code**
= _компилирует их в машинный код_

---

### **but it does not place the result**

- **but** — но (противопоставление)
- **it** — он / она / это
  👉 здесь **Go toolchain**
- **does not** — не делает
  👉 вспомогательный глагол
- **place** — размещать, класть
- **the result** — результат

📌
= _но он не размещает результат_

---

### **in the directory where your .go files are located**

- **in** — в
- **the directory** — папка, директория
- **where** — где
  👉 связывает части предложения
- **your** — твои
- **.go files** — go-файлы
- **are located** — находятся
  - **locate** — располагать

📌
= _в папке, где находятся твои .go-файлы_

---

## Предложение 2

### **Instead, Go quickly creates a temporary executable file**

- **Instead** — вместо этого
  👉 очень важное слово!
- **quickly** — быстро
- **creates** — создаёт
- **a** — неопределённый артикль
- **temporary** — временный
- **executable file** — исполняемый файл
  - **execute** — выполнять

📌
= _Вместо этого Go быстро создаёт временный исполняемый файл_

---

### **somewhere “for a short time”**

- **somewhere** — где-то
- **for** — на (период времени)
- **a short time** — короткое время

📌
= _где-то на короткое время_

---

### **(for example, in the /tmp directory,**

- **for example** — например
- **in** — в
- **/tmp directory** — папка /tmp

---

### **or it may even keep it only in memory)**

- **or** — или
- **it** — Go
- **may** — может
  👉 вероятность
- **even** — даже
- **keep** — держать, хранить
- **it** — его (файл)
- **only** — только
- **in memory** — в памяти (RAM)

📌
= _или он может даже держать его только в памяти_

---

## Предложение 3

### **As soon as the compiled program finishes its execution,**

- **As soon as** — как только
- **the compiled program** — скомпилированная программа
- **finishes** — завершает
- **its** — её
- **execution** — выполнение

📌
= _Как только скомпилированная программа завершает выполнение_

---

### **Go automatically deletes this temporary file.**

- **automatically** — автоматически
- **deletes** — удаляет
- **this** — этот
- **temporary file** — временный файл

---

## Предложение 4

### **In the directory with your .go source files,**

- **In** — в
- **the directory** — папке
- **with** — с
- **your** — твоими
- **source files** — исходниками

---

### **nothing remains except the source files themselves.**

- **nothing** — ничего
- **remains** — остаётся
- **except** — кроме
- **themselves** — сами по себе
  👉 усиление

📌
= _ничего не остаётся, кроме самих исходников_

---

## 💡 Главное, что ты должен вынести

1. **into** — превращение
2. **instead** — вместо этого
3. **as soon as** — как только
4. **may** — может (вероятность)
5. **it / them / themselves** — замена слов, чтобы не повторяться

---

</details>

---

### **Why are “persistent” and “temporary” important?**

**Persistent binary (`go build`):**

- You can run it as many times as you want without thinking about rebuilding it.
- It can be copied (for example, to another server or to a friend), stored in multiple copies, and digitally signed (for security).
- This is convenient when you have already “finished” the program and want it to exist as a standalone file.

**Temporary binary (`go run`):**

- It is convenient during development, when you frequently change the code and want to test it immediately.
- It does not leave any “clutter” in the project directory: each time it is “build, run, delete.”
- If you are running a small utility once or twice, there is no need to think about where to place the program file — Go cleans everything up automatically.

<details>
  <summary><b>📖 Подробное разборное объяснение (кликни)</b></summary>

### **Заголовок / Основной вопрос**

**Why are “persistent” and “temporary” important?**

- **Why** — **Почему**. Вопросительное наречие, начинает вопрос.
- **are** — форма глагола **to be** (быть) для множественного числа (**"persistent" and "temporary"** считаются как два понятия). **Являются**.
- **“persistent”** — **«постоянный», «сохраняющийся»**. От глагола "persist" (сохраняться, упорствовать). Ключевой термин: что-то, что существует долго, не стирается.
- **and** — **и**. Союз, соединяет два слова.
- **“temporary”** — **«временный»**. Антоним "persistent". Что-то, созданное на короткий срок.
- **important?** — **важными?** Прилагательное. Вопрос построен как `Why are X and Y important?` (Почему X и Y важны?).

**Перевод целиком:** **Почему «постоянный» и «временный» важны?**

---

### **Подзаголовок 1**

**Persistent binary (`go build`):**

- **Persistent** — **Постоянный**. Как и выше, это определение.
- **binary** — **бинарный файл, исполняемый файл**. Существительное. В контексте программирования — готовый файл программы, который можно запустить (например, `program.exe` в Windows).
- **(`go build`)** — это команда Go для сборки. В скобках указано, как создается такой файл.

**Перевод:** **Постоянный исполняемый файл (команда `go build`):**

---

### **Список (Persistent binary)**

**You can run it as many times as you want without thinking about rebuilding it.**

- **You** — **Ты / Вы**. Местоимение.
- **can run** — **можешь запускать**. Модальный глагол `can` (может) + основной глагол `run` (запускать).
- **it** — **его / его**. Местоимение, ссылается на "persistent binary".
- **as many times** — **столько раз**. Конструкция `as many` (столько) + `times` (разы).
- **as you want** — **сколько ты хочешь**. Сравнительная конструкция `as ... as` (такой ... как). `you want` (ты хочешь).
- **without** — **без**. Предлог.
- **thinking about** — **думая о**. Герундий (форма глагола на -ing, выступающая как существительное) от `think about` (думать о чем-то).
- **rebuilding** — **пересборки**. Герундий от `rebuild` (перестроить, пересобрать).
- **it** — **его**. Снова ссылается на бинарный файл.

**Перевод:** **Ты можешь запускать его сколько угодно раз, не думая о его пересборке.**

**It can be copied (for example, to another server or to a friend), stored in multiple copies, and digitally signed (for security).**

- **It** — **Он**. Местоимение.
- **can be copied** — **может быть скопирован**. Пассивный залог (объект подвергается действию): `can be` + причастие прошедшего времени `copied` (скопированный).
- **for example** — **например**. Вводное выражение.
- **to another server** — **на другой сервер**. Предлог `to` (направление) + `another` (другой) + `server` (сервер).
- **or** — **или**. Союз.
- **to a friend** — **другу**. `to` + `a friend` (друг).
- **stored** — **сохранен**. Причастие прошедшего времени от `store` (хранить). Здесь это продолжение списка в пассивном залоге: `can be copied, stored, and signed`.
- **in multiple copies** — **в множестве копий**. Предлог `in` (в) + `multiple` (множественные) + `copies` (копии).
- **and** — **и**.
- **digitally signed** — **цифрово подписан**. `digitally` (цифрово, наречие) + `signed` (подписанный, причастие).
- **for security** — **для безопасности**. Предлог `for` (для) + `security` (безопасность).

**Перевод:** **Он может быть скопирован (например, на другой сервер или другу), сохранен в множестве копий и цифрово подписан (для безопасности).**

**This is convenient when you have already “finished” the program and want it to exist as a standalone file.**

- **This** — **Это**. Указательное местоимение.
- **is convenient** — **является удобным**. Глагол-связка `is` + прилагательное `convenient` (удобный).
- **when** — **когда**. Союз.
- **you** — **ты**.
- **have already finished** — **уже закончил**. Present Perfect Tense: `have` + причастие `finished` (закончил). `Already` (уже) — наречие.
- **the program** — **программу**. Определенный артикль `the` + `program` (программа).
- **and** — **и**.
- **want** — **хочешь**. Глагол.
- **it** — **чтобы она**. Местоимение (программа).
- **to exist** — **существовала**. Инфинитив глагола `exist` (существовать). Конструкция `want something to do` (хотеть, чтобы что-то сделало).
- **as** — **в качестве**. Предлог.
- **a standalone file** — **самостоятельного файла**. Неопределенный артикль `a` + `standalone` (автономный, самостоятельный) + `file` (файл).

**Перевод:** **Это удобно, когда ты уже «закончил» программу и хочешь, чтобы она существовала в виде самостоятельного файла.**

---

### **Подзаголовок 2**

**Temporary binary (`go run`):**

- **Temporary** — **Временный**. Прилагательное, антоним "persistent".
- **binary** — **исполняемый файл**.
- **(`go run`)** — команда Go для запуска без явного создания файла.

**Перевод:** **Временный исполняемый файл (команда `go run`):**

---

### **Список (Temporary binary)**

**It is convenient during development, when you frequently change the code and want to test it immediately.**

- **It** — **Это**.
- **is convenient** — **удобно**.
- **during development** — **во время разработки**. Предлог `during` (в течение) + `development` (разработка).
- **when** — **когда**.
- **you** — **ты**.
- **frequently change** — **часто меняешь**. Наречие `frequently` (часто) + глагол `change` (менять).
- **the code** — **код**. `the` + `code`.
- **and** — **и**.
- **want to test** — **хочешь протестировать**. `want` + инфинитив `to test` (тестировать).
- **it** — **его** (код).
- **immediately** — **немедленно**. Наречие.

**Перевод:** **Это удобно во время разработки, когда ты часто меняешь код и хочешь сразу же его протестировать.**

**It does not leave any “clutter” in the project directory: each time it is “build, run, delete.”**

- **It** — **Оно** (временное действие `go run`).
- **does not leave** — **не оставляет**. Отрицательная форма Present Simple: вспомогательный глагол `does` + отрицательная частица `not` + основной глагол `leave` (оставлять).
- **any “clutter”** — **никакого «беспорядка»**. `any` (никакой, любой) + `clutter` (беспорядок, хлам). В кавычках, так это образное выражение.
- **in the project directory** — **в директории проекта**. `in` (в) + `the project` (проекта) + `directory` (папка, директория).
- **each time** — **каждый раз**.
- **it is** — **это есть**. `it` относится к процессу `go run`.
- **“build, run, delete.”** — **«собрали, запустили, удалили»**. Три глагола в форме повелительного наклонения или инфинитива, описывающие последовательность действий. В кавычках как слоган или описание цикла.

**Перевод:** **Оно не оставляет никакого «беспорядка» в папке проекта: каждый раз это «собрали, запустили, удалили».**

**If you are running a small utility once or twice, there is no need to think about where to place the program file — Go cleans everything up automatically.**

- **If** — **Если**. Союз условия.
- **you are running** — **ты запускаешь**. Present Continuous (подчеркивает действие в конкретный момент или как пример).
- **a small utility** — **небольшую утилиту**. `a small` (небольшую) + `utility` (служебная программа, утилита).
- **once or twice** — **один или два раза**. `once` (один раз), `twice` (два раза).
- **there is no need** — **нет необходимости**. Оборот `there is` (есть) + `no need` (никакой необходимости).
- **to think about** — **думать о**. Инфинитив с предлогом.
- **where to place** — **куда поместить**. Вопросительное слово `where` (куда) + инфинитив `to place` (поместить).
- **the program file** — **файл программы**.
- **Go** — **Go**. Название языка программирования.
- **cleans everything up** — **все очищает**. Фразовый глагол `clean up` (убирать, очищать). `Everything` (всё) — местоимение.
- **automatically** — **автоматически**. Наречие.

**Перевод:** **Если ты запускаешь небольшую утилиту один-два раза, нет необходимости думать, куда поместить файл программы — Go автоматически всё очищает.**

</details>

---

## **2. In detail: what happens during `go run`: “build + run” in one step**

Let’s break down step by step what the computer actually does when you type the following command in the console:

```bash
go run myprog.go
```

### **2.1. Step 1: Reading the source files (HDD/SSD → RAM)**

1. **Source file (.go)**
   - Suppose you have a file called `myprog.go` on your hard disk (HDD) or SSD. This is simply a text file containing Go code.
   - When you run `go run myprog.go`, the operating system (Windows, Linux, macOS) requests: “Open the file `myprog.go`.”
   - The computer reads this file **from storage** (HDD/SSD) and **loads its contents into random-access memory (RAM)**, because all further operations involve working with text and parsing code, and it is much more convenient to do this in RAM (it is faster there).

2. **Why RAM?**
   - RAM works several times faster than an HDD or even an SSD. That is why the entire “brain” of the compiler lives in RAM: it stores intermediate versions of your code, information about functions and variables, the dependency “graph” between different parts of the code, and so on.

<details>
  <summary><b>📖 Подробное разборное объяснение (кликни)</b></summary>

### **Заголовок раздела**

**## 2. In detail: what happens during `go run`: “build + run” in one step**

- **2.** — **2.** Номер раздела.
- **In detail:** — **Подробно:** Предлог `in` (в) + существительное `detail` (деталь). Фраза `in detail` означает «подробно, детально».
- **what happens** — **что происходит**. Вопросительное/относительное слово `what` (что) + глагол `happens` (происходит, 3-е лицо ед.ч. от `happen`).
- **during** — **во время, в течение**. Предлог, указывающий на период времени.
- **`go run`** — **команда `go run`**. Техническая команда. В тексте выделена моноширинным шрифтом (code font).
- **“build + run”** — **«сборка + запуск»**. Ключевая идея, заключена в кавычки. `build` (сборка), `+` (плюс), `run` (запуск).
- **in one step** — **в один шаг**. Предлог `in` (в) + `one` (один) + `step` (шаг, этап).

**Перевод и смысл:** **2. Подробно: что происходит во время выполнения `go run`: «сборка + запуск» в один шаг.** — Заголовок обещает детальное объяснение процесса, который скрыт за одной командой.

---

### **Вводный абзац**

**Let’s break down step by step what the computer actually does when you type the following command in the console:**

- **Let’s** — **Давайте**. Сокращение от `Let us` (Позволь нам). Используется для предложения совместного действия или объяснения.
- **break down** — **разберем**. Фразовый глагол. `Break` (ломать) + `down` (вниз). В переносном смысле — «разложить по полочкам», «анализировать», «разбить на части».
- **step by step** — **шаг за шагом**. Устойчивое выражение (adverbial phrase).
- **what** — **что**. Вводит объяснительную часть (object clause) — _что именно_ мы разберем.
- **the computer** — **компьютер**. Определенный артикль `the` (указывает на конкретный компьютер, который выполняет команду) + `computer`.
- **actually does** — **фактически делает**. Наречие `actually` (на самом деле, фактически) + глагол `does` (делает, 3-е лицо ед.ч. от `do`).
- **when** — **когда**. Союз, вводит обстоятельство времени.
- **you type** — **вы печатаете / вводите**. Местоимение `you` + глагол `type` (печатать, набирать на клавиатуре).
- **the following command** — **следующую команду**. Определенный артикль `the` + прилагательное `following` (следующая) + существительное `command` (команда).
- **in the console** — **в консоли**. Предлог `in` (в) + `the console` (консоль, командная строка, терминал).

**Перевод:** **Давайте разберем по шагам, что компьютер фактически делает, когда вы вводите следующую команду в консоли:**

**Код-блок:**

```bash
go run myprog.go
```

- **go** — сама программа-компилятор Go.
- **run** — **запустить**. Подкоманда (subcommand) для `go`.
- **myprog.go** — **мой_файл.go**. Имя файла с исходным кодом. `.go` — стандартное расширение для файлов на Go.

---

### **2.1. Step 1: Reading the source files (HDD/SSD → RAM)**

**1. Source file (.go)**

- **1.** — **1.** Номер пункта в списке.
- **Source file** — **Исходный файл**. `Source` (источник, исходный) + `file` (файл). Ключевое понятие в программировании.
- **(.go)** — расширение файла в скобках.

**Suppose you have a file called `myprog.go` on your hard disk (HDD) or SSD. This is simply a text file containing Go code.**

- **Suppose** — **Предположим**. Глагол в повелительном наклонении. Начинает гипотетическую или объяснительную ситуацию.
- **you have** — **у вас есть**. `You` + глагол `have` (иметь).
- **a file** — **файл**. Неопределенный артикль `a` + `file`.
- **called** — **называемый / с именем**. Причастие прошедшего времени от `call` (называть). Используется для определения: `a file called X` = «файл, называемый X».
- **`myprog.go`** — имя файла (пример).
- **on your hard disk (HDD) or SSD** — **на вашем жестком диске (HDD) или SSD**. Предлог `on` (на поверхности/в устройстве хранения) + `your` (вашем) + `hard disk` (жесткий диск). В скобках аббревиатура `HDD` и альтернатива `SSD` (твердотельный накопитель).
- **This** — **Это**. Местоимение, ссылается на упомянутый файл.
- **is** — **является**. Глагол-связка.
- **simply** — **просто**. Наречие, упрощает описание.
- **a text file** — **текстовый файл**. `A` + `text` (текстовый) + `file`.
- **containing** — **содержащий**. Причастие настоящего времени (present participle) от `contain` (содержать). Используется для создания определения: `a text file containing Go code` = «текстовый файл, _содержащий_ код Go».
- **Go code** — **код Go**. Название языка `Go` + `code` (код).

**Когда вы запускаете `go run myprog.go`, операционная система (Windows, Linux, macOS) запрашивает: "Открой файл `myprog.go`."**

- **When you run** — **Когда вы запускаете**. `When` (когда) + `you run` (вы запускаете). Обратите внимание: `run` здесь — основной глагол (запустить команду), а не часть `go run`.
- **`go run myprog.go`** — команда, которая запускается.
- **the operating system (Windows, Linux, macOS)** — **операционная система**. `The` + `operating` (операционная, действующая) + `system` (система). В скобках перечислены примеры популярных ОС.
- **requests** — **запрашивает**. Глагол от `request` (просить, запрашивать).
- **"Open the file `myprog.go`."** — **"Открой файл `myprog.go`."** Прямая речь, имитирующая команду ОС. `Open` (открой, повелительное наклонение) + `the file` (файл) + имя файла.

**Компьютер читает этот файл **из хранилища** (HDD/SSD) и **загружает его содержимое в оперативную память (RAM)**, потому что все последующие операции связаны с работой с текстом и разбором кода, и это гораздо удобнее делать в оперативной памяти (там быстрее).**

- **The computer** — **Компьютер**.
- **reads** — **читает**. Глагол.
- **this file** — **этот файл**. Указательное местоимение `this` (этот).
- **from storage** — **из хранилища**. Предлог `from` (из) + `storage` (хранилище, устройство хранения данных).
- **(HDD/SSD)** — уточнение в скобках.
- **and** — **и**.
- **loads its contents** — **загружает его содержимое**. `Loads` (загружает) + `its` (его, притяжательное местоимение для файла) + `contents` (содержимое, часто во множественном числе для данных).
- **into random-access memory (RAM)** — **в оперативную память (ОЗУ)**. Предлог `into` (в, внутрь) + технический термин `random-access memory` (память с произвольным доступом) + аббревиатура `RAM` в скобках.
- **because** — **потому что**. Союз, вводит причину.
- **all further operations** — **все последующие операции**. `All` (все) + `further` (дальнейшие, последующие) + `operations` (операции).
- **involve** — **включают (в себя), связаны с**. Глагол.
- **working with text** — **работу с текстом**. Герундий `working` (работа) + предлог `with` (с) + `text` (текст). Объект для глагола `involve` (`involve doing something` — включать в себя выполнение чего-либо).
- **and** — **и**.
- **parsing code** — **разбор (парсинг) кода**. Герундий `parsing` (синтаксический анализ, разбор) + `code`. `Parsing` — ключевое понятие в компиляции: анализ структуры текста кода согласно правилам языка.
- **and it is** — **и это (является)**. Союз `and` + местоимение `it` (это, ссылается на ситуацию) + `is`.
- **much more convenient** — **гораздо удобнее**. `Much more` (гораздо более) + прилагательное `convenient` (удобный). Сравнительная степень.
- **to do this** — **делать это**. Инфинитив `to do` (делать) + `this` (это).
- **in RAM** — **в ОЗУ**.
- **(it is faster there)** — **(там быстрее)**. Пояснение в скобках. `It` (ситуация/работа) + `is faster` (быстрее) + `there` (там, то есть в RAM).

---

### **2. Why RAM?**

**RAM works several times faster than an HDD or even an SSD.**

- **RAM** — **ОЗУ**. Подлежащее.
- **works** — **работает**. Глагол.
- **several times faster** — **в несколько раз быстрее**. `Several times` (несколько раз) + сравнительная степень наречия `faster` (быстрее).
- **than** — **чем**. Союз сравнения.
- **an HDD** — **жесткий диск (HDD)**. Неопределенный артикль `an` (перед гласным звуком [eɪʧ]) + `HDD`.
- **or** — **или**.
- **even an SSD** — **даже SSD**. `Even` (даже, усилительная частица) + `an SSD`.

**That is why the entire “brain” of the compiler lives in RAM: it stores intermediate versions of your code, information about functions and variables, the dependency “graph” between different parts of the code, and so on.**

- **That is why** — **Вот почему, Именно поэтому**. Устойчивое выражение (`That` (то) + `is why` (является причиной)). Вводит вывод из предыдущего утверждения.
- **the entire “brain”** — **весь «мозг»**. `The entire` (весь, целый) + `brain` (мозг) в кавычках (метафора).
- **of the compiler** — **компилятора**. Предлог `of` (принадлежности) + `the compiler` (компилятор — программа, переводящая код в бинарный файл).
- **lives** — **живет, находится**. Глагол, используется метафорически.
- **in RAM** — **в ОЗУ**.
- **:** — двоеточие, вводит поясняющий список.
- **it stores** — **он хранит**. `It` (компилятор/«мозг») + `stores` (хранит).
- **intermediate versions** — **промежуточные версии**. `Intermediate` (промежуточный) + `versions` (версии).
- **of your code** — **вашего кода**.
- **information about functions and variables** — **информацию о функциях и переменных**. `Information` (информация) + `about` (о) + `functions` (функции) + `and` (и) + `variables` (переменные).
- **the dependency “graph”** — **«граф» зависимостей**. `The dependency` (зависимость) + `graph` (граф — структура данных из узлов и связей) в кавычках (как технический термин).
- **between different parts** — **между различными частями**. Предлог `between` (между) + `different` (различными) + `parts` (частями).
- **of the code** — **кода**.
- **and so on** — **и так далее**. Устойчивое выражение, завершающее неполный список. Сокращенно `etc.`.

</details>
