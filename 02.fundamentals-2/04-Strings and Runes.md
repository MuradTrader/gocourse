Понял! Давайте пройдемся по **каждой строчке кода** и посмотрим **что происходит в памяти** и **что выводит программа**. Я буду объяснять буквально каждый шаг.

## **ЧАСТЬ 1: БАЗОВОЕ ПОНИМАНИЕ - БАЙТЫ В ПАМЯТИ**

### Код 1: Простая строка и ее байты

```go
package main

import "fmt"

func main() {
    // Создаем строку
    s := "Hello"

    // Что ВЫВОДИТ программа:
    fmt.Println("Строка s =", s)  // Вывод: Строка s = Hello

    // Посмотрим длину
    fmt.Println("len(s) =", len(s))  // Вывод: len(s) = 5

    // Посмотрим на каждый байт отдельно
    fmt.Println("\nПосмотрим на байты строки 'Hello':")
    for i := 0; i < len(s); i++ {
        fmt.Printf("s[%d] = байт %d (в шестнадцатеричном: 0x%02X) = символ '%c'\n",
            i, s[i], s[i], s[i])
    }
}
```

**Вывод программы:**

```
Строка s = Hello
len(s) = 5

Посмотрим на байты строки 'Hello':
s[0] = байт 72 (в шестнадцатеричном: 0x48) = символ 'H'
s[1] = байт 101 (в шестнадцатеричном: 0x65) = символ 'e'
s[2] = байт 108 (в шестнадцатеричном: 0x6C) = символ 'l'
s[3] = байт 108 (в шестнадцатеричном: 0x6C) = символ 'l'
s[4] = байт 111 (в шестнадцатеричном: 0x6F) = символ 'o'
```

**Объяснение:**

1. Когда мы пишем `s := "Hello"`, Go:

   - Выделяет память где-то в оперативной памяти (например, по адресу 0x1000)
   - Записывает туда байты: `[72, 101, 108, 108, 111]`
   - Создает переменную `s`, которая хранит:
     - Указатель на адрес 0x1000
     - Длину: 5 байт

2. `s[0]` - это **чтение из памяти**:

   - Go берет указатель из `s` (0x1000)
   - Добавляет 0
   - Читает 1 байт по адресу 0x1000
   - Получает значение 72

3. 72 в ASCII таблице = буква 'H'

## **ЧАСТЬ 2: РУССКИЕ БУКВЫ - В ЧЕМ РАЗНИЦА?**

### Код 2: Русская строка

```go
package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
    // Русская строка
    s := "Привет"

    fmt.Println("Строка s =", s)  // Вывод: Строка s = Привет
    fmt.Println("len(s) =", len(s))  // Вывод: len(s) = 12 (А НЕ 6!)

    fmt.Println("\nОШИБКА: Думаем, что это 6 букв, но...")
    fmt.Println("utf8.RuneCountInString(s) =", utf8.RuneCountInString(s))
    // Вывод: utf8.RuneCountInString(s) = 6

    fmt.Println("\nПосмотрим на байты:")
    for i := 0; i < len(s); i++ {
        fmt.Printf("s[%d] = байт %d (0x%02X)\n", i, s[i], s[i])
    }

    fmt.Println("\nПРОБЛЕМА: если попробовать вывести s[0] как символ:")
    fmt.Printf("s[0] как символ: '%c'\n", s[0])
    // Вывод: s[0] как символ: 'Ð' (это НЕ буква 'П'!)

    fmt.Println("\nПРАВИЛЬНО: используем range (он понимает UTF-8):")
    for i, r := range s {
        if i < 3 {  // Покажем только первые 3 символа
            fmt.Printf("Позиция %d: символ '%c' (руна %d, 0x%04X)\n",
                i, r, r, r)
        }
    }
}
```

**Вывод программы:**

```
Строка s = Привет
len(s) = 12

ОШИБКА: Думаем, что это 6 букв, но...
utf8.RuneCountInString(s) = 6

Посмотрим на байты:
s[0] = байт 208 (0xD0)
s[1] = байт 159 (0x9F)
s[2] = байт 209 (0xD1)
s[3] = байт 128 (0x80)
s[4] = байт 208 (0xD0)
s[5] = байт 184 (0xB8)
s[6] = байт 208 (0xD0)
s[7] = байт 178 (0xB2)
s[8] = байт 208 (0xD0)
s[9] = байт 181 (0xB5)
s[10] = байт 209 (0xD1)
s[11] = байт 130 (0x82)

ПРОБЛЕМА: если попробовать вывести s[0] как символ:
s[0] как символ: 'Ð' (это НЕ буква 'П'!)

ПРАВИЛЬНО: используем range (он понимает UTF-8):
Позиция 0: символ 'П' (руна 1055, 0x041F)
Позиция 2: символ 'р' (руна 1088, 0x0440)
Позиция 4: символ 'и' (руна 1080, 0x0438)
```

**Объяснение:**

1. Русская буква 'П' в Unicode имеет код 1055 (0x041F в шестнадцатеричном)
2. В UTF-8 она кодируется ДВУМЯ байтами: `0xD0 0x9F`
3. Поэтому `s[0]` дает только первый байт (0xD0), а это не полный символ!
4. `range` автоматически декодирует UTF-8 и дает нам правильные символы

## **ЧАСТЬ 3: РУНЫ - ЧТО ЭТО ТАКОЕ?**

### Код 3: Что такое руны на самом деле

```go
package main

import "fmt"

func main() {
    // Создаем руны
    r1 := 'A'  // Английская буква
    r2 := 'П'  // Русская буква
    r3 := '😊' // Эмодзи

    fmt.Println("=== РУНЫ - ЭТО ПРОСТО ЧИСЛА ===")

    fmt.Println("\n1. Английская 'A':")
    fmt.Printf("r1 = '%c'\n", r1)      // Вывод: r1 = 'A'
    fmt.Printf("Значение r1: %d\n", r1) // Вывод: Значение r1: 65
    fmt.Printf("Тип r1: %T\n", r1)     // Вывод: Тип r1: int32

    fmt.Println("\n2. Русская 'П':")
    fmt.Printf("r2 = '%c'\n", r2)      // Вывод: r2 = 'П'
    fmt.Printf("Значение r2: %d\n", r2) // Вывод: Значение r2: 1055
    fmt.Printf("Тип r2: %T\n", r2)     // Вывод: Тип r2: int32

    fmt.Println("\n3. Эмодзи '😊':")
    fmt.Printf("r3 = '%c'\n", r3)      // Вывод: r3 = '😊'
    fmt.Printf("Значение r3: %d\n", r3) // Вывод: Значение r3: 128522
    fmt.Printf("Тип r3: %T\n", r3)     // Вывод: Тип r3: int32

    fmt.Println("\n=== ДОКАЗАТЕЛЬСТВО, ЧТО РУНЫ = ЧИСЛА ===")

    // Создаем число
    var num int32 = 65  // То же самое, что 'A'
    fmt.Printf("\nnum = %d\n", num)                // Вывод: num = 65
    fmt.Printf("rune(num) = '%c'\n", rune(num))   // Вывод: rune(num) = 'A'

    // Можно делать арифметику!
    r4 := 'A' + 1
    fmt.Printf("\n'A' + 1 = %c (код %d)\n", r4, r4)
    // Вывод: 'A' + 1 = B (код 66)

    // Преобразуем руны в строки
    fmt.Println("\n=== ПРЕОБРАЗОВАНИЕ РУН В СТРОКИ ===")
    s1 := string(r1)  // Руна 'A' в строку
    s2 := string(r2)  // Руна 'П' в строку
    s3 := string(r3)  // Руна '😊' в строку

    fmt.Printf("string('A') = %s\n", s1)    // Вывод: string('A') = A
    fmt.Printf("string('П') = %s\n", s2)    // Вывод: string('П') = П
    fmt.Printf("string('😊') = %s\n", s3)   // Вывод: string('😊') = 😊
    fmt.Printf("Длина строки с эмодзи: %d байт\n", len(s3))
    // Вывод: Длина строки с эмодзи: 4 байта
}
```

**Вывод программы:**

```
=== РУНЫ - ЭТО ПРОСТО ЧИСЛА ===

1. Английская 'A':
r1 = 'A'
Значение r1: 65
Тип r1: int32

2. Русская 'П':
r2 = 'П'
Значение r2: 1055
Тип r2: int32

3. Эмодзи '😊':
r3 = '😊'
Значение r3: 128522
Тип r3: int32

=== ДОКАЗАТЕЛЬСТВО, ЧТО РУНЫ = ЧИСЛА ===

num = 65
rune(num) = 'A'

'A' + 1 = B (код 66)

=== ПРЕОБРАЗОВАНИЕ РУН В СТРОКИ ===
string('A') = A
string('П') = П
string('😊') = 😊
Длина строки с эмодзи: 4 байт
```

## **ЧАСТЬ 4: КАК UTF-8 РАБОТАЕТ ВНУТРИ**

### Код 4: Кодирование рун в UTF-8

```go
package main

import (
    "fmt"
    "unicode/utf8"
)

func main() {
    fmt.Println("=== КАК UTF-8 КОДИРУЕТ РАЗНЫЕ СИМВОЛЫ ===\n")

    // Тестовые руны
    runes := []rune{'A', 'П', '世', '😊'}

    for _, r := range runes {
        // Кодируем руну в UTF-8
        buf := make([]byte, 4)  // Максимальная длина UTF-8 символа - 4 байта
        n := utf8.EncodeRune(buf, r)

        fmt.Printf("Руна: %c (Unicode: U+%04X, значение: %d)\n", r, r, r)
        fmt.Printf("Занимает в UTF-8: %d байт\n", n)

        fmt.Printf("Байты в памяти: ")
        for i := 0; i < n; i++ {
            fmt.Printf("0x%02X ", buf[i])
        }
        fmt.Println()

        fmt.Printf("Бинарное представление: ")
        for i := 0; i < n; i++ {
            fmt.Printf("%08b ", buf[i])
        }
        fmt.Println("\n")
    }

    fmt.Println("=== ПРАВИЛА UTF-8 ===")
    fmt.Println("1. Если символ ASCII (0-127): 1 байт, начинается с 0")
    fmt.Println("2. Если 2 байта: начинается с 110xxxxx, затем 10xxxxxx")
    fmt.Println("3. Если 3 байта: начинается с 1110xxxx, затем 10xxxxxx 10xxxxxx")
    fmt.Println("4. Если 4 байта: начинается с 11110xxx, затем 10xxxxxx 10xxxxxx 10xxxxxx")
}
```

**Вывод программы:**

```
=== КАК UTF-8 КОДИРУЕТ РАЗНЫЕ СИМВОЛЫ ===

Руна: A (Unicode: U+0041, значение: 65)
Занимает в UTF-8: 1 байт
Байты в памяти: 0x41
Бинарное представление: 01000001

Руна: П (Unicode: U+041F, значение: 1055)
Занимает в UTF-8: 2 байт
Байты в памяти: 0xD0 0x9F
Бинарное представление: 11010000 10011111

Руна: 世 (Unicode: U+4E16, значение: 19990)
Занимает в UTF-8: 3 байт
Байты в памяти: 0xE4 0xB8 0x96
Бинарное представление: 11100100 10111000 10010110

Руна: 😊 (Unicode: U+1F60A, значение: 128522)
Занимает в UTF-8: 4 байт
Байты в памяти: 0xF0 0x9F 0x98 0x8A
Бинарное представление: 11110000 10011111 10011000 10001010

=== ПРАВИЛА UTF-8 ===
1. Если символ ASCII (0-127): 1 байт, начинается с 0
2. Если 2 байта: начинается с 110xxxxx, затем 10xxxxxx
3. Если 3 байта: начинается с 1110xxxx, затем 10xxxxxx 10xxxxxx
4. Если 4 байта: начинается с 11110xxx, затем 10xxxxxx 10xxxxxx 10xxxxxx
```

## **ЧАСТЬ 5: ПРАКТИЧЕСКИЕ ПРИМЕРЫ И ОШИБКИ**

### Код 5: Частые ошибки и как их избежать

```go
package main

import (
    "fmt"
    "strings"
    "unicode/utf8"
)

func main() {
    fmt.Println("=== ПРИМЕР 1: Неправильный подсчет длины ===")

    s1 := "Hello"
    fmt.Printf("s1 = \"%s\"\n", s1)                     // Вывод: s1 = "Hello"
    fmt.Printf("len(s1) = %d\n", len(s1))               // Вывод: len(s1) = 5
    fmt.Printf("Правильно? ДА, потому что 5 букв = 5 байт\n\n")

    s2 := "Привет"
    fmt.Printf("s2 = \"%s\"\n", s2)                     // Вывод: s2 = "Привет"
    fmt.Printf("len(s2) = %d\n", len(s2))               // Вывод: len(s2) = 12
    fmt.Printf("Правильно? НЕТ! Мы видим 6 букв, но len() говорит 12\n")
    fmt.Printf("Потому что каждая русская буква = 2 байта\n")
    fmt.Printf("Правильный способ: utf8.RuneCountInString(s2) = %d\n\n",
        utf8.RuneCountInString(s2))                     // Вывод: 6

    fmt.Println("=== ПРИМЕР 2: Неправильное получение символа ===")

    s3 := "Привет"
    fmt.Printf("s3 = \"%s\"\n", s3)                     // Вывод: s3 = "Привет"
    fmt.Printf("s3[0] = %d (0x%02X)\n", s3[0], s3[0])  // Вывод: s3[0] = 208 (0xD0)
    fmt.Printf("s3[0] как символ: '%c'\n", s3[0])      // Вывод: s3[0] как символ: 'Ð'
    fmt.Printf("Это НЕ буква 'П'! Это только первый байт!\n\n")

    fmt.Println("Правильные способы:")
    // Способ 1: преобразовать в []rune
    runes := []rune(s3)
    fmt.Printf("[]rune(s3)[0] = '%c'\n", runes[0])      // Вывод: 'П'

    // Способ 2: использовать range
    for i, r := range s3 {
        if i == 0 {
            fmt.Printf("range: первый символ = '%c'\n", r) // Вывод: 'П'
            break
        }
    }

    fmt.Println("\n=== ПРИМЕР 3: Обрезание строки ===")

    s4 := "Hello, 世界!"
    fmt.Printf("s4 = \"%s\"\n", s4)                     // Вывод: s4 = "Hello, 世界!"
    fmt.Printf("len(s4) = %d\n", len(s4))               // Вывод: например, 13

    // ОШИБКА: обрезаем по байтам
    badCut := s4[:9]  // 9 байт
    fmt.Printf("\nНЕПРАВИЛЬНО: s4[:9] = \"%s\"\n", badCut)
    // Вывод: "Hello, �" - обрезали посреди китайского иероглифа!

    // ПРАВИЛЬНО: обрезаем по символам
    runes2 := []rune(s4)
    goodCut := string(runes2[:8])  // 8 символов
    fmt.Printf("ПРАВИЛЬНО: string([]rune(s4)[:8]) = \"%s\"\n", goodCut)
    // Вывод: "Hello, 世"

    fmt.Println("\n=== ПРИМЕР 4: Конкатенация в цикле ===")

    // МЕДЛЕННЫЙ способ
    var slowString string
    for i := 0; i < 5; i++ {
        slowString += "a"
    }
    fmt.Printf("Медленный способ: \"%s\"\n", slowString)  // Вывод: "aaaaa"
    fmt.Println("Проблема: создается новая строка на каждой итерации!\n")

    // БЫСТРЫЙ способ
    var builder strings.Builder
    for i := 0; i < 5; i++ {
        builder.WriteString("a")
    }
    fastString := builder.String()
    fmt.Printf("Быстрый способ: \"%s\"\n", fastString)  // Вывод: "aaaaa"
    fmt.Println("Правильно: strings.Builder создает строку эффективно!")
}
```

**Вывод программы:**

```
=== ПРИМЕР 1: Неправильный подсчет длины ===
s1 = "Hello"
len(s1) = 5
Правильно? ДА, потому что 5 букв = 5 байт

s2 = "Привет"
len(s2) = 12
Правильно? НЕТ! Мы видим 6 букв, но len() говорит 12
Потому что каждая русская буква = 2 байта
Правильный способ: utf8.RuneCountInString(s2) = 6

=== ПРИМЕР 2: Неправильное получение символа ===
s3 = "Привет"
s3[0] = 208 (0xD0)
s3[0] как символ: 'Ð'
Это НЕ буква 'П'! Это только первый байт!

Правильные способы:
[]rune(s3)[0] = 'П'
range: первый символ = 'П'

=== ПРИМЕР 3: Обрезание строки ===
s4 = "Hello, 世界!"
len(s4) = 13

НЕПРАВИЛЬНО: s4[:9] = "Hello, �"
ПРАВИЛЬНО: string([]rune(s4)[:8]) = "Hello, 世"

=== ПРИМЕР 4: Конкатенация в цикле ===
Медленный способ: "aaaaa"
Проблема: создается новая строка на каждой итерации!

Быстрый способ: "aaaaa"
Правильно: strings.Builder создает строку эффективно!
```

## **КРАТКАЯ ШПАРГАЛКА:**

1. **Английские буквы** = 1 байт

   - `"Hello"` = 5 байт
   - `s[0]` = 'H' (правильно)

2. **Русские буквы** = 2 байта

   - `"Привет"` = 12 байт (6 букв × 2 байта)
   - `s[0]` = 208 (это НЕ буква 'П', а только ее первая половина!)

3. **Китайские иероглифы** = 3 байта

   - `"世界"` = 6 байт (2 символа × 3 байта)

4. **Эмодзи** = 4 байта

   - `"😊"` = 4 байта (1 символ)

5. **Правила работы:**
   - Для подсчета символов: `utf8.RuneCountInString(s)`
   - Для перебора символов: `for i, r := range s`
   - Для обрезания: сначала в `[]rune`, потом обрезать
   - Для конкатенации в цикле: `strings.Builder`

**САМОЕ ГЛАВНОЕ:**

- `len(s)` всегда возвращает **байты**, не символы
- `s[i]` всегда возвращает **байт**, не символ
- `range` по строке автоматически декодирует UTF-8 и возвращает **символы**
